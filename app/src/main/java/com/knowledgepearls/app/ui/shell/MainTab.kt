package com.knowledgepearls.app.ui.shell

import com.knowledgepearls.app.ui.theme.TabTheme

enum class MainTab(val theme: TabTheme) {
    Feed(TabTheme.Feed),
    Folders(TabTheme.Folders),
    PublicFeed(TabTheme.PublicFeed),
    Favourites(TabTheme.Favourites),
}
