# Built application files
*.apk
*.aar
*.ap_
*.aab

# Gradle
.gradle/
build/
**/build/

# Local configuration
local.properties
gradle-jdk.properties
gradle/config.properties
*.iml
.idea/
.DS_Store

# Android Studio captures
captures/
.externalNativeBuild/
.cxx/

# Keystores — never commit
*.jks
*.keystore
keystore.properties

# Firebase (add when created on MacBook)
# google-services.json is project-specific — commit only if using public anon config

# Local tooling / sync artifacts — never commit or mirror into builds
.tmp-deps/
.kotlin/

# Secrets
secrets.properties
